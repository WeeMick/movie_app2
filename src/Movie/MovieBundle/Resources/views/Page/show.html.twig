{% extends 'MovieMovieBundle::layout.html.twig' %}
{% block title %}{{ movie.title }}{% endblock%}
{% block body %}
    <h2>{{ movie.id }}</h2>
    <h1> {{ movie.title }}</h1>
    <p>{{ movie.summary }}</p>
    <hr>
    <a href="/">Go back</a>
{% endblock %}