{% extends 'MovieMovieBundle::layout.html.twig' %}
{% block title %}Edit Movie{% endblock%}
{% block body %}
    <header>
        <h1>Edit {{  movie.title }}</h1>
    </header>
    <form action="" method="post">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </form>

    <button class="btn btn-danger"><a href="{{ path('movie_index') }}">Cancel Editing Movie</a></button>
{% endblock %}